<%= form_for @endpoint do |f| %>
  <% if endpoint.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(endpoint.errors.count, "error") %> prohibited this endpoint from being saved:</h2>

      <ul>
      <% endpoint.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>

  <% end %>
  <div>
    <%= f.label :slug %>
    <%= f.text_field :slug %>
  </div>
  <div>
    <%= f.label :sql_statement %>
    <%= f.text_area :sql_statement %>
  </div>
  <div class="acttitleions">
    <%= f.submit %>
  </div>
<% end %>
